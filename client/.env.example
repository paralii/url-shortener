# URL of your Express API (no trailing slash)
VITE_API_URL=http://localhost:1122/api

# Base used to construct the full short URL shown to the user
# Usually the same as VITE_API_URL in local dev,
# but in production this would be your short domain: https://shr.t
VITE_SHORT_BASE=http://localhost:1122/api
